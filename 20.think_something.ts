// Create an array of countries
const countries: string[] = ["USA", "Canada", "Australia", "Japan", "Brazil"];

// Print the list of countries
console.log("List of countries:");
countries.forEach(country => {
    console.log(country);
});
